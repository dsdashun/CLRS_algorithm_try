#ifndef _SORT_DATA_HELPER
#define _SORT_DATA_HELPER

void dumpData(double data[], size_t n);
unsigned char checkData(double data[], size_t n);
int prepareData(double **pDataArray, size_t *pSize);

#endif
